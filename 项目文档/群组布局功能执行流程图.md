# 群组布局功能执行流程图

```mermaid
flowchart TD
    A[开始布局流程] --> B{选择布局模式}
    
    B --> C[普通布局]
    B --> D[群组内部布局]
    B --> E[递归布局]
    
    C --> C1[获取顶层节点]
    C1 --> C2[应用网格中心布局]
    C2 --> C3[解决节点碰撞]
    C3 --> C4[更新嵌套节点位置]
    C4 --> C5[优化边连接点]
    C5 --> C6[返回布局结果]
    
    D --> D1[获取目标群组]
    D1 --> D2[获取子节点]
    D2 --> D3[计算内部相对坐标]
    D3 --> D4[转换为绝对坐标]
    D4 --> D5[约束节点边界]
    D5 --> D6[更新嵌套节点位置]
    D6 --> D7[更新群组边界]
    D7 --> D8[优化边连接点]
    D8 --> C6
    
    E --> E1[构建嵌套树]
    E1 --> E2[获取最大深度]
    E2 --> E3[从最深层开始]
    E3 --> E4[遍历每层群组]
    E4 --> E5[调用单群组布局]
    E5 --> E6[记录位置]
    E6 --> E7[布局顶层节点]
    E7 --> E8[调整父群组大小]
    E8 --> E9[优化边连接点]
    E9 --> C6
    
    C6 --> F[UI组件接收结果]
    F --> G[设置isLayoutMode为true]
    G --> H[批量更新节点位置]
    H --> I[更新边连接点]
    I --> J{群组尺寸变化}
    J -->|是| K[触发updateGroupBoundary]
    J -->|否| L[延迟触发updateGroupBoundary]
    K --> L
    L --> M[更新父群组边界]
    M --> N[设置isLayoutMode为false]
    N --> O[结束]

    subgraph 核心算法
        P[GridCenterLayoutStrategy]
        Q[getTargetNodes]
        R[calculateGridCenterPositions]
        S[resolveCollisions]
        T[updateNestedNodePositions]
    end

    subgraph 边界管理
        V[GroupBoundaryOperations]
        W[updateSingleGroupBoundary]
        X[calculateChildrenRelativeBounds]
    end

    subgraph 状态管理
        Z[Zustand Store]
        AA[useGraphStore]
        BB[isLayoutMode]
    end
    
    P -.-> C2
    V -.-> M
    Z -.-> G
```

## 流程说明

### 1. 布局模式选择
- **普通布局**: 对整个画布进行网格布局
- **群组内部布局**: 仅对选中的群组内部节点进行布局
- **递归布局**: 从最深层群组开始，逐层向上对所有嵌套群组进行布局

### 2. 布局算法核心
- **GridCenterLayoutStrategy**: 主要的布局策略
- 包括目标节点获取、网格位置计算、碰撞解决等步骤

### 3. 群组边界管理
- **updateGroupBoundary**: 更新群组边界以包含所有子节点
- **防抖处理**: 避免频繁更新导致性能问题
- **递归更新**: 自动更新父群组边界

### 4. 状态管理
- **isLayoutMode**: 在布局过程中临时禁用约束逻辑
- **节点位置更新**: 批量更新以提高性能
- **历史记录**: 记录布局操作以便撤销/重做