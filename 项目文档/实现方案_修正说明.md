# 多画布管理实现方案 - 修正说明

> 基于现有源码分析，对《多画布管理_实现方案.md》中的数据结构设计进行修正

---

## 1. 关键修正点

### 1.1 时间戳类型：string → Date

**现有代码使用 `Date` 类型：**

```typescript
// src/types/graph/models.ts (现有代码)
export interface Node {
  createdAt: Date  // ✅ 使用 Date 类型
  updatedAt: Date
}

export const NodeSchema = z.object({
  createdAt: z.date(),  // ✅ 使用 z.date()
  updatedAt: z.date(),
})
```

**需要修正的部分（实现方案文档中）：**

```typescript
// ❌ 错误（实现方案文档）
export const UserSchema = z.object({
  createdAt: z.string(),  // 应改为 z.date()
})

export const CanvasSchema = z.object({
  createdAt: z.string(),  // 应改为 z.date()
  updatedAt: z.string(),  // 应改为 z.date()
})

// ✅ 正确
export const UserSchema = z.object({
  createdAt: z.date(),
})

export const CanvasSchema = z.object({
  createdAt: z.date(),
  updatedAt: z.date(),
})
```

---

## 2. 修正后的数据结构定义

### 2.1 src/types/workspace/models.ts（修正版）

```typescript
import { z } from 'zod'
import { Node, Group, Edge, NodeSchema, EdgeSchema } from '@/types/graph/models'

// ========== 用户模型 (预留设计) ==========
export interface User {
  id: string
  name: string
  avatar?: string
  createdAt: Date  // ✅ 修正：使用 Date
}

export const UserSchema = z.object({
  id: z.string(),
  name: z.string(),
  avatar: z.string().optional(),
  createdAt: z.date(),  // ✅ 修正：使用 z.date()
})

// ========== 画布模型 ==========
export interface Canvas {
  id: string
  name: string
  parentId: string | null
  children: string[]  // 子画布 ID 列表
  graphData: {
    nodes: (Node | Group)[]  // ✅ 修正：包含 Node 和 Group
    edges: Edge[]
  }
  viewportState: {
    x: number
    y: number
    zoom: number
  }
  isCollapsed: boolean
  createdAt: Date  // ✅ 修正：使用 Date
  updatedAt: Date  // ✅ 修正：使用 Date
}

export const CanvasSchema = z.object({
  id: z.string(),
  name: z.string().min(1),
  parentId: z.string().nullable(),
  children: z.array(z.string()),
  graphData: z.object({
    nodes: z.array(NodeSchema),  // ⚠️ 这里简化，实际需要 z.union([NodeSchema, GroupSchema])
    edges: z.array(EdgeSchema),
  }),
  viewportState: z.object({
    x: z.number(),
    y: z.number(),
    zoom: z.number(),
  }),
  isCollapsed: z.boolean(),
  createdAt: z.date(),  // ✅ 修正：使用 z.date()
  updatedAt: z.date(),  // ✅ 修正：使用 z.date()
})

// ========== 画布树节点 ==========
export interface CanvasTreeNode {
  id: string
  name: string
  isCollapsed: boolean
  children: CanvasTreeNode[]
}

export const CanvasTreeNodeSchema: z.ZodType<CanvasTreeNode> = z.lazy(() =>
  z.object({
    id: z.string(),
    name: z.string(),
    isCollapsed: z.boolean(),
    children: z.array(CanvasTreeNodeSchema),
  })
)

// ========== 工作空间模型 ==========
export interface Workspace {
  userId: string
  currentCanvasId: string
  canvases: Canvas[]
  canvasTree: CanvasTreeNode[]
}

export const WorkspaceSchema = z.object({
  userId: z.string(),
  currentCanvasId: z.string(),
  canvases: z.array(CanvasSchema),
  canvasTree: z.array(CanvasTreeNodeSchema),
})

// ========== 默认值 ==========
export const DEFAULT_USER: User = {
  id: 'user_0',
  name: '默认用户',
  createdAt: new Date(),  // ✅ 修正：使用 Date 对象
}

export const DEFAULT_CANVAS: Canvas = {
  id: 'canvas_default',
  name: '默认画布',
  parentId: null,
  children: [],
  graphData: {
    nodes: [],
    edges: [],
  },
  viewportState: {
    x: 0,
    y: 0,
    zoom: 1,
  },
  isCollapsed: false,
  createdAt: new Date(),  // ✅ 修正：使用 Date 对象
  updatedAt: new Date(),  // ✅ 修正：使用 Date 对象
}
```

---

### 2.2 src/types/workspace/storage.ts（修正版）

```typescript
import { z } from 'zod'
import { WorkspaceSchema } from './models'

// ========== 存储数据版本 ==========
export const STORAGE_VERSION = '1.0.0'

// ========== 存储键常量 ==========
export const STORAGE_KEYS = {
  WORKSPACE: 'kg-editor:workspace',
  VERSION: 'kg-editor:version',
} as const

// ========== 存储数据格式 ==========
export interface StorageData {
  version: string
  workspace: Workspace
  timestamp: Date  // ✅ 修正：使用 Date
}

export const StorageDataSchema = z.object({
  version: z.string(),
  workspace: WorkspaceSchema,
  timestamp: z.date(),  // ✅ 修正：使用 z.date()
})

// ========== 存储适配器接口 ==========
export interface StorageAdapter {
  save(key: string, data: unknown): Promise<void>
  load<T>(key: string): Promise<T | null>
  remove(key: string): Promise<void>
  clear(): Promise<void>
}
```

---

## 3. LocalStorage 序列化处理

### 3.1 问题说明

localStorage 只能存储字符串，因此需要处理 `Date` 类型的序列化和反序列化。

### 3.2 解决方案

**方案A：使用自定义序列化器（推荐）**

```typescript
// src/services/storage/adapters/LocalStorageAdapter.ts

export class LocalStorageAdapter implements StorageAdapter {
  /**
   * 序列化数据（Date → ISO string）
   */
  private serialize(data: unknown): string {
    return JSON.stringify(data, (key, value) => {
      // 将 Date 对象转换为 ISO 字符串
      if (value instanceof Date) {
        return { __type: 'Date', value: value.toISOString() }
      }
      return value
    })
  }

  /**
   * 反序列化数据（ISO string → Date）
   */
  private deserialize<T>(serialized: string): T {
    return JSON.parse(serialized, (key, value) => {
      // 将标记的 Date 字符串转换回 Date 对象
      if (value && value.__type === 'Date') {
        return new Date(value.value)
      }
      return value
    })
  }

  async save(key: string, data: unknown): Promise<void> {
    try {
      const serialized = this.serialize(data)
      localStorage.setItem(key, serialized)
    } catch (error) {
      if (error instanceof Error && error.name === 'QuotaExceededError') {
        throw new Error('存储空间不足，请清理部分数据')
      }
      throw error
    }
  }

  async load<T>(key: string): Promise<T | null> {
    try {
      const serialized = localStorage.getItem(key)
      if (!serialized) return null
      return this.deserialize<T>(serialized)
    } catch (error) {
      console.error(`加载数据失败 [${key}]:`, error)
      return null
    }
  }

  async remove(key: string): Promise<void> {
    localStorage.removeItem(key)
  }

  async clear(): Promise<void> {
    localStorage.clear()
  }
}
```

**方案B：使用 superjson 库（可选）**

```typescript
import superjson from 'superjson'

export class LocalStorageAdapter implements StorageAdapter {
  async save(key: string, data: unknown): Promise<void> {
    const serialized = superjson.stringify(data)
    localStorage.setItem(key, serialized)
  }

  async load<T>(key: string): Promise<T | null> {
    const serialized = localStorage.getItem(key)
    if (!serialized) return null
    return superjson.parse<T>(serialized)
  }
}
```

---

## 4. StorageManager 修正

```typescript
// src/services/storage/StorageManager.ts

import {
  StorageAdapter,
  StorageData,
  StorageDataSchema,
  STORAGE_KEYS,
  STORAGE_VERSION,
} from '@/types/workspace/storage'
import { Workspace, Canvas } from '@/types/workspace/models'
import { LocalStorageAdapter } from './adapters/LocalStorageAdapter'

export class StorageManager {
  private adapter: StorageAdapter

  constructor(adapter?: StorageAdapter) {
    this.adapter = adapter || new LocalStorageAdapter()
  }

  /**
   * 加载完整工作空间数据
   */
  async loadWorkspace(): Promise<Workspace | null> {
    try {
      const data = await this.adapter.load<StorageData>(STORAGE_KEYS.WORKSPACE)
      if (!data) return null

      // ⚠️ 验证数据格式（Date 类型已经由适配器反序列化）
      const validatedData = StorageDataSchema.parse(data)

      // 检查版本兼容性
      if (validatedData.version !== STORAGE_VERSION) {
        console.warn('数据版本不匹配，可能需要迁移')
        // TODO: 调用数据迁移服务
      }

      return validatedData.workspace
    } catch (error) {
      console.error('加载工作空间失败:', error)
      return null
    }
  }

  /**
   * 保存完整工作空间数据
   */
  async saveWorkspace(workspace: Workspace): Promise<void> {
    try {
      const storageData: StorageData = {
        version: STORAGE_VERSION,
        workspace,
        timestamp: new Date(),  // ✅ 使用 Date 对象
      }

      await this.adapter.save(STORAGE_KEYS.WORKSPACE, storageData)
    } catch (error) {
      console.error('保存工作空间失败:', error)
      throw error
    }
  }

  /**
   * 保存单个画布数据
   */
  async saveCanvas(workspace: Workspace, canvasId: string, canvas: Canvas): Promise<void> {
    try {
      // 更新画布
      const updatedCanvases = workspace.canvases.map((c) =>
        c.id === canvasId ? { ...canvas, updatedAt: new Date() } : c  // ✅ 使用 Date 对象
      )

      const updatedWorkspace: Workspace = {
        ...workspace,
        canvases: updatedCanvases,
      }

      await this.saveWorkspace(updatedWorkspace)
    } catch (error) {
      console.error('保存画布失败:', error)
      throw error
    }
  }

  // ... 其他方法
}

// 导出单例
export const storageManager = new StorageManager()
```

---

## 5. canvasSlice 修正

```typescript
// src/stores/workspace/canvasSlice.ts

createCanvas: (name, parentId) => {
  const newCanvas: Canvas = {
    id: `canvas_${nanoid()}`,
    name,
    parentId: parentId || null,
    children: [],
    graphData: {
      nodes: [],
      edges: [],
    },
    viewportState: {
      x: 0,
      y: 0,
      zoom: 1,
    },
    isCollapsed: false,
    createdAt: new Date(),  // ✅ 使用 Date 对象
    updatedAt: new Date(),  // ✅ 使用 Date 对象
  }

  // ... 其他逻辑
},

renameCanvas: (canvasId, newName) => {
  set((state) => ({
    canvases: state.canvases.map((c) =>
      c.id === canvasId
        ? { ...c, name: newName, updatedAt: new Date() }  // ✅ 使用 Date 对象
        : c
    ),
    // ...
  }))
},
```

---

## 6. 修正总结

### 6.1 全局修正点

| 位置 | 错误 | 修正 |
|------|------|------|
| **类型定义** | `createdAt: string` | `createdAt: Date` |
| **Zod Schema** | `z.string()` | `z.date()` |
| **默认值** | `new Date().toISOString()` | `new Date()` |
| **LocalStorage** | 直接 `JSON.stringify()` | 自定义序列化器 |

### 6.2 需要修正的文件（实现方案文档中）

1. `src/types/workspace/models.ts` - 所有时间戳字段
2. `src/types/workspace/storage.ts` - `timestamp` 字段
3. `src/services/storage/adapters/LocalStorageAdapter.ts` - 新增序列化/反序列化
4. `src/services/storage/StorageManager.ts` - 所有 `new Date()` 调用
5. `src/stores/workspace/canvasSlice.ts` - 所有 `new Date()` 调用

---

## 7. 最佳实践

### 7.1 时间戳处理

```typescript
// ✅ 创建新数据
const canvas: Canvas = {
  createdAt: new Date(),
  updatedAt: new Date(),
}

// ✅ 更新数据
const updatedCanvas = {
  ...canvas,
  updatedAt: new Date(),
}

// ✅ 格式化显示
const formatted = canvas.createdAt.toLocaleDateString()
const iso = canvas.createdAt.toISOString()
```

### 7.2 序列化测试

```typescript
// 测试序列化/反序列化
const testData: StorageData = {
  version: '1.0.0',
  workspace: mockWorkspace,
  timestamp: new Date(),
}

const adapter = new LocalStorageAdapter()
await adapter.save('test', testData)
const loaded = await adapter.load<StorageData>('test')

expect(loaded?.timestamp).toBeInstanceOf(Date)
expect(loaded?.workspace.canvases[0].createdAt).toBeInstanceOf(Date)
```

---

## 8. 迁移建议

### 8.1 如果现有数据使用 string 类型

如果将来从其他系统迁移数据，需要转换函数：

```typescript
/**
 * 将旧格式数据转换为新格式
 */
function migrateCanvas(oldCanvas: any): Canvas {
  return {
    ...oldCanvas,
    createdAt: new Date(oldCanvas.createdAt),  // string → Date
    updatedAt: new Date(oldCanvas.updatedAt),
  }
}
```

### 8.2 版本检测

```typescript
async loadWorkspace(): Promise<Workspace | null> {
  const data = await this.adapter.load<any>(STORAGE_KEYS.WORKSPACE)

  // 检测旧版本数据
  if (data && typeof data.workspace?.canvases[0]?.createdAt === 'string') {
    console.log('检测到旧版本数据，开始迁移...')
    return migrateOldData(data)
  }

  return StorageDataSchema.parse(data).workspace
}
```

---

**文档版本**: v1.0
**创建时间**: 2025-12-09
**状态**: 待应用到实现方案
