# 布局系统重构 - 执行流程详解

## 📋 文档说明

本文档详细说明三种布局场景的完整执行流程，基于真实源码编写。

**最后更新**：2025-11-21

---

## 🎯 三种典型布局场景

1. **画布整体布局**：用户点击"应用布局"按钮
2. **群组内部布局**：用户选中群组后点击"布局群组内部"
3. **递归全局布局**：用户点击"递归布局全部"按钮

每种场景使用不同的策略，但遵循统一的流程框架。

---

## 📐 场景1：画布整体布局

### 用户操作

```
用户操作：点击"应用布局"按钮
触发函数：LayoutControl.handleLayout()
```

### 完整执行流程

#### 第1步：UI层准备（LayoutControl.tsx）

**禁用交互**：
```
setIsProcessing(true)
setSelectedNodeId(null)
```

**获取数据**：
```
nodes = useGraphStore.getNodes()
edges = useGraphStore.getEdges()
```

**创建管理器**：
```
layoutManager = new LayoutManager()
```

**发起布局请求**：
```
layoutResult = await layoutManager.applyLayout(nodes, edges, {
  animate: true,
  useWeightedLayout: true
})
```

---

#### 第2步：智能策略选择（LayoutManager）

**选择逻辑**：
```
检查 options.strategy -> 无
检查 options.layoutMode -> 无
检查 options.targetGroupId -> 无
默认选择: 'canvas-layout'
```

**获取策略实例**：
```
strategy = this.strategies.get('canvas-layout')
// 返回 CanvasLayoutStrategy 实例
```

---

#### 第3步：策略执行 - 筛选顶层节点（CanvasLayoutStrategy）

**筛选条件**：
```
topLevelNodes = nodes.filter(node =>
  !node.groupId
)
```

**示例数据**：
```
输入节点（10个）：
  NodeA (无groupId)       ✅ 顶层
  NodeB (无groupId)       ✅ 顶层
  GroupX (无groupId)      ✅ 顶层
  NodeC (groupId: GroupX) ❌ 嵌套
  NodeD (groupId: GroupX) ❌ 嵌套
  GroupY (无groupId)      ✅ 顶层
  NodeE (groupId: GroupY) ❌ 嵌套
  NodeF (groupId: GroupY) ❌ 嵌套
  NodeG (groupId: GroupY) ❌ 嵌套
  NodeH (无groupId)       ✅ 顶层

筛选结果（5个）：
  [NodeA, NodeB, GroupX, GroupY, NodeH]
```

---

#### 第4步：计算节点权重

**权重计算流程**：

**4.1 统计边连接**：
```
for (edge of edges) {
  if (edge.source === node.id) outEdges++
  if (edge.target === node.id) inEdges++
}
totalEdges = inEdges + outEdges
```

**4.2 计算面积权重**：
```
area = node.width * node.height
areaWeight = min(1, area / 100000)
```

**4.3 计算边数权重**：
```
totalEdgeWeight = min(1, totalEdges / 10)
inEdgeWeight = min(1, inEdges / 10)
outEdgeWeight = min(1, outEdges / 10)
```

**4.4 综合权重**：
```
weight = areaWeight * 0.2
       + totalEdgeWeight * 0.3
       + inEdgeWeight * 0.25
       + outEdgeWeight * 0.25
```

**示例计算**：
```
NodeA:
  尺寸: 120x80 = 9600
  总边: 5条, 入边: 3条, 出边: 2条

  areaWeight = min(1, 9600/100000) = 0.096
  totalEdgeWeight = min(1, 5/10) = 0.5
  inEdgeWeight = min(1, 3/10) = 0.3
  outEdgeWeight = min(1, 2/10) = 0.2

  weight = 0.096*0.2 + 0.5*0.3 + 0.3*0.25 + 0.2*0.25
         = 0.294

权重排名（假设）：
  GroupY: 0.520 （最重要，中心位置）
  GroupX: 0.410
  NodeA:  0.294
  NodeB:  0.180
  NodeH:  0.120
```

---

#### 第5步：算法计算布局（GridCenterAlgorithm）

**5.1 权重排序**：
```
排序后顺序：
  [GroupY, GroupX, NodeA, NodeB, NodeH]
```

**5.2 计算网格参数**：
```
nodeCount = 5
optimalCols = ceil(sqrt(5)) = 3
rows = ceil(5 / 3) = 2

网格结构：
  Row 0: [GroupY, GroupX, NodeA]
  Row 1: [NodeB, NodeH]
```

**5.3 计算网格总尺寸**：
```
假设：
  avgWidth = 150
  avgHeight = 100
  horizontalSpacing = 150
  verticalSpacing = 100

gridWidth = 150*3 + 150*2 = 750
gridHeight = 100*2 + 100*1 = 300
```

**5.4 计算居中偏移**：
```
offsetX = -750 / 2 = -375
offsetY = -300 / 2 = -150
```

**5.5 计算每个节点位置**：
```
GroupY (row=0, col=0):
  x = -375 + 0*300 + 75 = -300
  y = -150 + 0*200 + 50 = -100
  位置: (-300, -100)

GroupX (row=0, col=1):
  x = -375 + 1*300 + 75 = 0
  y = -150 + 0*200 + 50 = -100
  位置: (0, -100)  // 中心位置

NodeA (row=0, col=2):
  x = -375 + 2*300 + 75 = 300
  y = -150 + 0*200 + 50 = -100
  位置: (300, -100)

NodeB (row=1, col=0):
  x = -375 + 0*300 + 75 = -300
  y = -150 + 1*200 + 50 = 100
  位置: (-300, 100)

NodeH (row=1, col=1):
  x = -375 + 1*300 + 75 = 0
  y = -150 + 1*200 + 50 = 100
  位置: (0, 100)
```

**布局效果**：
```
        (-300,-100)  (0,-100)    (300,-100)
          GroupY     GroupX       NodeA
                       ↑
                   最高权重
                   中心位置

        (-300,100)   (0,100)
          NodeB       NodeH
```

---

#### 第6步：碰撞检测与解决（CollisionResolver）

**6.1 检测碰撞**：
```
collisionCount = 0
for (i = 0; i < nodes.length; i++) {
  for (j = i+1; j < nodes.length; j++) {
    if (isColliding(nodes[i], nodes[j])) {
      collisionCount++
    }
  }
}
```

**6.2 迭代解决**：
```
假设检测到1个碰撞（NodeB和NodeH距离过近）

iteration = 0
while (iteration < 50 && hasCollisions()) {
  解决碰撞（推开节点）
  iteration++
}

解决后：
  NodeB: (-300, 100) -> (-320, 100)  向左推
  NodeH: (0, 100) -> (20, 100)       向右推
```

---

#### 第7步：更新嵌套节点位置

**7.1 识别嵌套节点**：
```
nestedNodes = nodes.filter(n => n.groupId)

结果：
  NodeC (groupId: GroupX)
  NodeD (groupId: GroupX)
  NodeE (groupId: GroupY)
  NodeF (groupId: GroupY)
  NodeG (groupId: GroupY)
```

**7.2 计算绝对位置**：

**NodeC（属于GroupX）**：
```
原始：
  GroupX: (200, 150)
  NodeC: (220, 170)  // 相对GroupX: (20, 20)

布局后：
  GroupX: (0, -100)  // 新位置

计算：
  relativeX = 220 - 200 = 20
  relativeY = 170 - 150 = 20

  newX = 0 + 20 = 20
  newY = -100 + 20 = -80

  NodeC新位置: (20, -80)
```

**NodeE（属于GroupY）**：
```
原始：
  GroupY: (500, 300)
  NodeE: (520, 320)  // 相对GroupY: (20, 20)

布局后：
  GroupY: (-300, -100)  // 新位置

计算：
  relativeX = 520 - 500 = 20
  relativeY = 320 - 300 = 20

  newX = -300 + 20 = -280
  newY = -100 + 20 = -80

  NodeE新位置: (-280, -80)
```

**最终所有节点位置**：
```
顶层节点（5个）：
  GroupY: (-300, -100)
  GroupX: (0, -100)
  NodeA: (300, -100)
  NodeB: (-320, 100)
  NodeH: (20, 100)

嵌套节点（5个）：
  NodeC: (20, -80)      // 跟随GroupX
  NodeD: (50, -80)      // 跟随GroupX
  NodeE: (-280, -80)    // 跟随GroupY
  NodeF: (-250, -80)    // 跟随GroupY
  NodeG: (-280, -50)    // 跟随GroupY
```

---

#### 第8步：优化边连接点（EdgeOptimizer）

**8.1 遍历所有边**：
```
for (edge of edges) {
  sourceNode = 找到源节点
  targetNode = 找到目标节点

  bestHandles = 选择最优连接点(sourceNode, targetNode)

  edge.sourceHandle = bestHandles.source
  edge.targetHandle = bestHandles.target
}
```

**8.2 选择最优连接点**：
```
示例边: NodeA -> NodeB

NodeA位置: (300, -100)
NodeB位置: (-320, 100)

尝试16种组合：
  top->top, top->bottom, top->left, top->right
  bottom->top, bottom->bottom, ...
  ...

计算每种组合的距离，选择最短的：
  NodeA.left -> NodeB.right
  距离最短，选择这个组合

结果：
  edge.sourceHandle = 'left'
  edge.targetHandle = 'right'
```

---

#### 第9步：返回结果（CanvasLayoutStrategy）

**构建结果Map**：
```
nodePositions = new Map([
  ['GroupY', { x: -300, y: -100 }],
  ['GroupX', { x: 0, y: -100 }],
  ['NodeA', { x: 300, y: -100 }],
  ['NodeB', { x: -320, y: 100 }],
  ['NodeH', { x: 20, y: 100 }],
  ['NodeC', { x: 20, y: -80 }],
  ['NodeD', { x: 50, y: -80 }],
  ['NodeE', { x: -280, y: -80 }],
  ['NodeF', { x: -250, y: -80 }],
  ['NodeG', { x: -280, y: -50 }]
])

edgeHandles = new Map([
  ['edge1', { sourceHandle: 'left', targetHandle: 'right', ... }],
  ...
])

return {
  success: true,
  nodes: nodePositions,
  edges: edgeHandles,
  errors: [],
  stats: {
    duration: 125,
    iterations: 1,
    collisions: 1
  }
}
```

---

#### 第10步：UI更新（LayoutControl.tsx）

**10.1 启用布局模式**：
```
useGraphStore.getState().setIsLayoutMode(true)
```

**10.2 更新节点位置**：
```
for ([nodeId, position] of layoutResult.nodes) {
  updateNode(nodeId, { position })
}
```

**10.3 更新边连接点**：
```
for ([edgeId, edgeData] of layoutResult.edges) {
  updateEdge(edgeId, {
    sourceHandle: edgeData.sourceHandle,
    targetHandle: edgeData.targetHandle
  })
}
```

**10.4 额外边优化**：
```
延迟执行（确保节点位置已更新）：
  EdgeOptimizer.optimizeEdgeHandles(finalNodes, finalEdges)
  再次更新边连接点
```

**10.5 恢复交互**：
```
setIsLayoutMode(false)
setIsProcessing(false)
```

---

### 时间线总结

```
0ms:    用户点击按钮
1ms:    UI禁用交互，获取数据
2ms:    创建LayoutManager
3ms:    选择CanvasLayoutStrategy
5ms:    筛选5个顶层节点
10ms:   计算节点权重
15ms:   GridCenterAlgorithm计算布局
20ms:   碰撞检测（发现1个）
30ms:   碰撞解决（迭代3次）
40ms:   更新5个嵌套节点位置
50ms:   优化10条边的连接点
60ms:   构建返回结果
70ms:   UI更新10个节点位置
80ms:   UI更新10条边连接点
100ms:  额外边优化
120ms:  恢复交互，完成
```

---

## 🔷 场景2：群组内部布局

### 用户操作

```
用户操作：
  1. 选中群组GroupX
  2. 点击"布局群组内部"按钮

触发函数：LayoutControl.handleGroupLayout()
```

### 完整执行流程

#### 第1步：UI层准备

**验证选中状态**：
```
if (!selectedNodeId || !isGroupSelected) {
  console.warn('未选中群组')
  return
}

if (childrenCount === 0) {
  console.warn('群组无子节点')
  return
}
```

**发起布局请求**：
```
layoutResult = await layoutManager.applyLayout(nodes, edges, {
  targetGroupId: selectedNodeId,  // 'GroupX'
  layoutScope: 'group',
  animate: true,
  useWeightedLayout: true
})
```

---

#### 第2步：智能策略选择

**选择逻辑**：
```
检查 options.strategy -> 无
检查 options.layoutMode -> 无
检查 options.targetGroupId -> 'GroupX'  ✅
选择: 'group-layout'
```

**获取策略**：
```
strategy = this.strategies.get('group-layout')
// 返回 GroupLayoutStrategy 实例
```

---

#### 第3步：策略执行 - 查找目标群组

**查找群组**：
```
parentGroup = nodes.find(n =>
  n.id === 'GroupX' &&
  n.type === 'group'
)

结果：
  GroupX {
    id: 'GroupX',
    type: 'group',
    position: { x: 500, y: 300 },
    width: 400,
    height: 300,
    ...
  }
```

---

#### 第4步：获取并去重子节点

**筛选子节点**：
```
children = nodes.filter(n =>
  'groupId' in n &&
  n.groupId === 'GroupX'
)

结果（假设）：
  [NodeC, NodeD, NodeE, NodeC]  // NodeC重复
```

**去重处理**：
```
uniqueMap = new Map()
uniqueMap.set('NodeC', NodeC)
uniqueMap.set('NodeD', NodeD)
uniqueMap.set('NodeE', NodeE)

children = [NodeC, NodeD, NodeE]  // 3个节点
```

**日志输出**：
```
console.log(`布局GroupX的3个子节点`)
```

---

#### 第5步：算法计算相对布局

**调用GridAlgorithm**：
```
relativeLayout = GridAlgorithm.calculate(children, {
  horizontalSpacing: 150,
  verticalSpacing: 100
})
```

**计算过程**：
```
nodeCount = 3
optimalCols = ceil(sqrt(3)) = 2
rows = ceil(3 / 2) = 2

网格：
  Row 0: [NodeC, NodeD]
  Row 1: [NodeE]

位置计算：
  NodeC (row=0, col=0):
    x = 0*270 + 60 = 60
    y = 0*180 + 40 = 40

  NodeD (row=0, col=1):
    x = 1*270 + 60 = 330
    y = 0*180 + 40 = 40

  NodeE (row=1, col=0):
    x = 0*270 + 60 = 60
    y = 1*180 + 40 = 220

相对布局结果：
  NodeC: (60, 40)
  NodeD: (330, 40)
  NodeE: (60, 220)
```

---

#### 第6步：转换为绝对坐标

**计算群组锚点**：
```
GroupX位置: (500, 300)
padding: { left: 20, top: 20 }

anchor = {
  x: 500 + 20 = 520,
  y: 300 + 20 = 320
}
```

**转换坐标**：
```
NodeC: (60, 40) + (520, 320) = (580, 360)
NodeD: (330, 40) + (520, 320) = (850, 360)
NodeE: (60, 220) + (520, 320) = (580, 540)
```

---

#### 第7步：碰撞检测与解决

**检测**：
```
collisionCount = CollisionResolver.countCollisions([NodeC, NodeD, NodeE])
// 假设: 0个碰撞
```

**无需解决**，跳过。

---

#### 第8步：自适应调整群组大小

**8.1 计算子节点边界**：
```
NodeC: 位置(580, 360), 尺寸120x80
  左: 580 - 60 - 500 = 20
  右: 580 + 60 - 500 = 140
  上: 360 - 40 - 300 = 20
  下: 360 + 40 - 300 = 100

NodeD: 位置(850, 360), 尺寸120x80
  左: 850 - 60 - 500 = 290
  右: 850 + 60 - 500 = 410
  上: 360 - 40 - 300 = 20
  下: 360 + 40 - 300 = 100

NodeE: 位置(580, 540), 尺寸120x80
  左: 580 - 60 - 500 = 20
  右: 580 + 60 - 500 = 140
  上: 540 - 40 - 300 = 200
  下: 540 + 40 - 300 = 280

整体边界：
  minX = 20
  maxX = 410
  minY = 20
  maxY = 280

  子节点宽度 = 410 - 20 = 390
  子节点高度 = 280 - 20 = 260
```

**8.2 计算新群组尺寸**：
```
padding: { left: 20, top: 20, right: 20, bottom: 20 }
EXTRA_MARGIN = 20

newWidth = 390 + 20 + 20 + 20 = 450
newHeight = 260 + 20 + 20 + 20 = 320

GroupX.width = 450
GroupX.height = 320
```

**8.3 更新边界信息**：
```
GroupX.boundary = {
  left: 500,
  right: 500 + 450 = 950,
  top: 300,
  bottom: 300 + 320 = 620
}
```

---

#### 第9步：返回结果

**构建结果**：
```
nodePositions = new Map([
  ['NodeC', { x: 580, y: 360 }],
  ['NodeD', { x: 850, y: 360 }],
  ['NodeE', { x: 580, y: 540 }],
  ['GroupX', {
    x: 500,
    y: 300,
    width: 450,
    height: 320,
    boundary: { ... }
  }]
])

return {
  success: true,
  nodes: nodePositions,
  edges: new Map(),  // 群组布局不处理边
  errors: [],
  stats: {
    duration: 45,
    iterations: 1,
    collisions: 0
  }
}
```

---

#### 第10步：UI更新

**更新节点位置**：
```
updateNode('NodeC', { position: { x: 580, y: 360 } })
updateNode('NodeD', { position: { x: 850, y: 360 } })
updateNode('NodeE', { position: { x: 580, y: 540 } })
```

**更新群组尺寸**：
```
updateNode('GroupX', {
  width: 450,
  height: 320,
  boundary: { ... }
})
```

**触发群组边界更新**：
```
updateGroupBoundary('GroupX')
```

**额外边优化**：
```
EdgeOptimizer.optimizeEdgeHandles(finalNodes, finalEdges)
updateEdge(...)
```

---

### 时间线总结

```
0ms:    用户点击按钮
1ms:    验证选中状态
2ms:    创建LayoutManager
3ms:    选择GroupLayoutStrategy
5ms:    查找GroupX
7ms:    筛选3个子节点
10ms:   去重处理
15ms:   GridAlgorithm计算相对布局
20ms:   转换为绝对坐标
25ms:   碰撞检测（0个）
30ms:   计算子节点边界
35ms:   调整GroupX尺寸
40ms:   构建返回结果
45ms:   UI更新节点位置
50ms:   UI更新群组尺寸
60ms:   触发边界更新
70ms:   额外边优化
80ms:   完成
```

---

## 🔁 场景3：递归全局布局

### 用户操作

```
用户操作：点击"递归布局全部"按钮
触发函数：LayoutControl.handleRecursiveLayout()
```

### 完整执行流程

#### 第1步：构建嵌套层级树

**输入数据（假设）**：
```
节点结构：
  GroupA (depth 0)
    ├─ NodeA1 (depth 1)
    └─ GroupB (depth 1)
        ├─ NodeB1 (depth 2)
        └─ NodeB2 (depth 2)

  GroupC (depth 0)
    └─ NodeC1 (depth 1)

  NodeX (depth 0)
  NodeY (depth 0)
```

**构建深度分组**：
```
depthGroups = NestingTreeBuilder.groupNodesByDepth(nodes)

结果：
  Map {
    0 => [GroupA, GroupC, NodeX, NodeY],
    1 => [NodeA1, GroupB, NodeC1],
    2 => [NodeB1, NodeB2]
  }

maxDepth = 2
```

**日志输出**：
```
console.log('检测到 3 层嵌套结构，最大深度: 2')
```

---

#### 第2步：深度2布局（最深层）

**获取深度2的群组**：
```
groupsAtDepth2 = getGroupsAtDepth(depthGroups, 2)
// 结果: [] （深度2没有群组，只有节点）
```

**跳过**，因为没有群组需要布局。

---

#### 第3步：深度1布局

**获取深度1的群组**：
```
groupsAtDepth1 = getGroupsAtDepth(depthGroups, 1)
// 结果: [GroupB]
```

**布局GroupB**：

**3.1 检查子节点**：
```
children = [NodeB1, NodeB2]  // 2个子节点
```

**3.2 调用GroupLayoutStrategy**：
```
result = await GroupLayoutStrategy.applyLayout(workingNodes, edges, {
  targetGroupId: 'GroupB'
})
```

**3.3 执行流程**（与场景2相同）：
```
相对布局:
  NodeB1: (60, 40)
  NodeB2: (330, 40)

转换绝对坐标（假设GroupB位置: 200, 150）:
  锚点: (200+20, 150+20) = (220, 170)
  NodeB1: (280, 210)
  NodeB2: (550, 210)

调整GroupB尺寸:
  新尺寸: 450 x 150
```

**3.4 合并结果**：
```
workingNodes中更新:
  NodeB1.position = (280, 210)
  NodeB2.position = (550, 210)
  GroupB.width = 450
  GroupB.height = 150
```

**3.5 进度回调**：
```
onProgress({
  currentLevel: 2,
  totalLevels: 3,
  processedNodes: 2,
  totalNodes: 8
})

控制台输出:
  📊 布局进度: 深度 2/3, 已处理 2/8 个节点
```

---

#### 第4步：深度0布局（群组层）

**获取深度0的群组**：
```
groupsAtDepth0 = getGroupsAtDepth(depthGroups, 0)
// 结果: [GroupA, GroupC]
```

**布局GroupA**：

**4.1 检查子节点**：
```
children = [NodeA1, GroupB]  // 2个子节点（含嵌套群组）
```

**4.2 调用GroupLayoutStrategy**：
```
result = await GroupLayoutStrategy.applyLayout(workingNodes, edges, {
  targetGroupId: 'GroupA'
})
```

**4.3 执行流程**：
```
相对布局:
  NodeA1: (60, 40)
  GroupB: (330, 40)  // GroupB现在尺寸是450x150

转换绝对坐标（假设GroupA位置: 100, 100）:
  锚点: (120, 120)
  NodeA1: (180, 160)
  GroupB: (450, 160)

调整GroupA尺寸:
  需要包含GroupB（450x150）
  新尺寸: 600 x 280
```

**4.4 更新嵌套节点位置**：
```
GroupB移动了: (200, 150) -> (450, 160)
偏移: (250, 10)

NodeB1跟随移动:
  旧位置: (280, 210)
  新位置: (280+250, 210+10) = (530, 220)

NodeB2跟随移动:
  旧位置: (550, 210)
  新位置: (550+250, 210+10) = (800, 220)
```

**布局GroupC**：

**4.5 类似流程**：
```
children = [NodeC1]

相对布局:
  NodeC1: (60, 40)

转换绝对坐标:
  锚点: (600+20, 300+20) = (620, 320)
  NodeC1: (680, 360)

调整GroupC尺寸:
  新尺寸: 200 x 180
```

**4.6 进度回调**：
```
onProgress({
  currentLevel: 3,
  totalLevels: 3,
  processedNodes: 6,
  totalNodes: 8
})
```

---

#### 第5步：布局顶层节点

**调用CanvasLayoutStrategy**：
```
result = await CanvasLayoutStrategy.applyLayout(workingNodes, edges, {
  useWeightedLayout: true
})
```

**执行流程**：

**5.1 筛选顶层节点**：
```
topLevelNodes = [GroupA, GroupC, NodeX, NodeY]
```

**5.2 计算权重**：
```
假设权重排序:
  GroupA: 0.650 （最大）
  GroupC: 0.420
  NodeX:  0.180
  NodeY:  0.150
```

**5.3 GridCenterAlgorithm布局**：
```
optimalCols = ceil(sqrt(4)) = 2
rows = 2

网格:
  Row 0: [GroupA, GroupC]
  Row 1: [NodeX, NodeY]

居中布局（假设）:
  GroupA: (-400, -200)
  GroupC: (100, -200)
  NodeX:  (-400, 150)
  NodeY:  (100, 150)
```

**5.4 更新所有嵌套节点位置**：
```
GroupA移动: (100, 100) -> (-400, -200)
偏移: (-500, -300)

NodeA1跟随:
  (180, 160) -> (180-500, 160-300) = (-320, -140)

GroupB跟随:
  (450, 160) -> (450-500, 160-300) = (-50, -140)

NodeB1、NodeB2跟随GroupB移动:
  NodeB1: (530, 220) -> (30, -80)
  NodeB2: (800, 220) -> (300, -80)

GroupC移动: (600, 300) -> (100, -200)
偏移: (-500, -500)

NodeC1跟随:
  (680, 360) -> (180, -140)
```

---

#### 第6步：从深到浅调整所有群组大小

**调整深度1群组（GroupB）**：
```
已在第3步调整过，检查是否需要再次调整
可能因为父群组移动需要微调
```

**调整深度0群组（GroupA, GroupC）**：
```
GroupA需要包含:
  NodeA1: (-320, -140)
  GroupB: (-50, -140)  尺寸450x150

重新计算GroupA尺寸:
  可能需要扩大以完整包含GroupB
  新尺寸: 650 x 300

GroupC需要包含:
  NodeC1: (180, -140)

重新计算GroupC尺寸:
  新尺寸: 220 x 180
```

---

#### 第7步：优化所有边

**调用EdgeOptimizer**：
```
optimizedEdges = EdgeOptimizer.optimizeEdgeHandles(
  adjustedNodes,
  edges
)

优化所有类型的边:
  • GroupA <-> GroupC
  • NodeA1 <-> NodeB1
  • NodeX <-> NodeY
  • ...
```

---

#### 第8步：返回结果

**构建完整结果Map**：
```
nodePositions = new Map([
  // 顶层节点
  ['GroupA', { x: -400, y: -200, width: 650, height: 300, ... }],
  ['GroupC', { x: 100, y: -200, width: 220, height: 180, ... }],
  ['NodeX', { x: -400, y: 150 }],
  ['NodeY', { x: 100, y: 150 }],

  // 深度1节点
  ['NodeA1', { x: -320, y: -140 }],
  ['GroupB', { x: -50, y: -140, width: 450, height: 150, ... }],
  ['NodeC1', { x: 180, y: -140 }],

  // 深度2节点
  ['NodeB1', { x: 30, y: -80 }],
  ['NodeB2', { x: 300, y: -80 }]
])

edgeHandles = new Map([所有优化后的边])

stats = {
  duration: 280,
  iterations: 4,  // GroupB + GroupA + GroupC + Canvas
  collisions: 2
}
```

**日志输出**：
```
✅ 递归布局完成！
   • 处理了 3 层嵌套结构
   • 更新了 8 个节点
   • 优化了 12 条边
   • 总迭代次数: 4
   • 碰撞处理次数: 2
   • 耗时: 280ms
```

---

#### 第9步：UI更新

**更新所有节点位置和尺寸**：
```
for ([nodeId, posData] of layoutResult.nodes) {
  updateData = { position: { x: posData.x, y: posData.y } }

  if (posData.width) updateData.width = posData.width
  if (posData.height) updateData.height = posData.height
  if (posData.boundary) updateData.boundary = posData.boundary

  updateNode(nodeId, updateData)
}
```

**更新所有边**：
```
for ([edgeId, edgeData] of layoutResult.edges) {
  updateEdge(edgeId, {
    sourceHandle: edgeData.sourceHandle,
    targetHandle: edgeData.targetHandle
  })
}
```

**触发所有群组边界更新**：
```
setTimeout(() => {
  updateGroupBoundary('GroupA')
  updateGroupBoundary('GroupB')
  updateGroupBoundary('GroupC')
}, 100)
```

---

### 时间线总结

```
0ms:    用户点击按钮
5ms:    构建深度分组（3层）
10ms:   深度2: 无群组，跳过
20ms:   深度1: 布局GroupB（2节点）
50ms:   进度: 2/3, 2/8节点
60ms:   深度0: 布局GroupA（2节点含GroupB）
100ms:  深度0: 布局GroupC（1节点）
140ms:  进度: 3/3, 6/8节点
150ms:  画布: 布局4个顶层节点
200ms:  画布: 更新所有嵌套节点位置
220ms:  从深到浅调整所有群组大小
250ms:  优化所有边（12条）
280ms:  构建返回结果
290ms:  UI更新8个节点
310ms:  UI更新12条边
330ms:  触发群组边界更新
350ms:  完成
```

---

## 📊 三种场景对比

| 特性 | 画布布局 | 群组布局 | 递归布局 |
|------|---------|---------|---------|
| **处理节点数** | 5顶层 + 5嵌套 | 3群组内 | 8全部 |
| **使用策略** | Canvas | Group | Recursive(组合) |
| **使用算法** | GridCenter | Grid | 两者都用 |
| **迭代次数** | 1次 | 1次 | 4次 |
| **碰撞处理** | 1次 | 0次 | 2次 |
| **边优化** | 2次 | 1次 | 1次 |
| **耗时** | 120ms | 80ms | 350ms |
| **进度回调** | ❌ | ❌ | ✅ |
| **更新群组尺寸** | ❌ | ✅ 单个 | ✅ 所有 |

---

## 🎯 关键流程节点

### 所有场景共有的步骤

1. **UI准备**：禁用交互，获取数据
2. **策略选择**：智能选择合适策略
3. **碰撞解决**：迭代推开重叠节点
4. **构建结果**：创建Map返回
5. **UI更新**：更新节点和边

### 画布布局特有

1. **权重计算**：基于面积和边数
2. **权重排序**：高权重节点靠中心
3. **更新嵌套位置**：子节点跟随移动
4. **居中布局**：整体居中在原点

### 群组布局特有

1. **子节点去重**：防止重复节点
2. **固定锚点**：群组左上角+padding
3. **相对→绝对转换**：两步坐标转换
4. **自适应尺寸**：自动扩大群组

### 递归布局特有

1. **深度分组**：按嵌套层级分组
2. **从深到浅**：逐层布局群组
3. **进度回调**：实时报告进度
4. **级联更新**：嵌套节点级联移动
5. **全局优化**：优化所有边

---

**下一步阅读**：[05-智能策略选择.md](./05-智能策略选择.md)
