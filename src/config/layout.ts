/**
 * 布局配置文件
 *
 * 开发人员可在此集中管理所有布局相关的参数，
 * 包括间距、尺寸、网格配置等。
 */

// ============================================================
// 容器内边距
// ============================================================

/**
 * 容器内边距 - 适用于Group和作为父节点的Node
 *
 * - top: 标题栏高度 + 额外间距
 * - left/right/bottom: 边距
 */
export const CONTAINER_PADDING = {
  top: 70,
  left: 20,
  right: 20,
  bottom: 20,
} as const;

// ============================================================
// 视觉边距
// ============================================================

/**
 * 节点视觉边距 - 用于计算边界时考虑阴影、边框等视觉效果
 */
export const NODE_VISUAL_PADDING = 4;

// ============================================================
// 节点尺寸定义
// ============================================================

/**
 * 节点默认尺寸配置
 */
export const NODE_DIMENSIONS = {
  /**
   * 普通笔记节点 (NoteNode)
   */
  NOTE: {
    collapsed: { width: 350, height: 280 },  // 收缩状态
    expanded: { width: 600, height: 450 },   // 展开状态
  },

  /**
   * 群组节点 (GroupNode)
   */
  GROUP: {
    default: { width: 300, height: 200 },    // 默认尺寸
    min: { width: 300, height: 200 },        // 最小尺寸
  },
} as const;

// ============================================================
// 布局算法配置
// ============================================================

/**
 * 网格布局配置
 *
 * - columns: 每行显示的节点数量
 * - spacingX: 水平间距（节点宽度 + 间隙）
 * - spacingY: 垂直间距（节点高度 + 间隙）
 */
export const GRID_LAYOUT = {
  columns: 2,          // 每行2个节点
  spacingX: 380,       // 350px(节点宽) + 30px(间距)
  spacingY: 310,       // 280px(节点高) + 30px(间距)
} as const;

// ============================================================
// 布局算法类型
// ============================================================

/**
 * 支持的布局算法类型
 */
export const LAYOUT_ALGORITHMS = {
  GRID: 'grid',
  // 未来扩展:
  // FORCE: 'force',
  // TREE: 'tree',
  // CIRCLE: 'circle',
} as const;

export type LayoutAlgorithmType = typeof LAYOUT_ALGORITHMS[keyof typeof LAYOUT_ALGORITHMS];
